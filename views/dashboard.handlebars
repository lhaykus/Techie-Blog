<div class="mb-5 pt-3">        
        <h2 class="title pb-4" style="color:aqua">Dashboard: Your Posts </h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <div style="background-color: black;
    font-size: 1rem;">
    <br></br>
            <a href="/dashboard/edit/{{post.id}}"><button class="button is-success is-outlined mb-5 ml-5">Edit Post</button></a></div>
        </div>
    {{/each}}

{{else}}
<h6 class="subtitle is-5 pb-4 has-text-centered" style="color:blueviolet">Click the "Create Post" button to create your first post!</h6>

{{/if}}